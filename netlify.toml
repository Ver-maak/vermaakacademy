# Netlify deployment config for the TanStack Start app.
# The Vite Netlify adapter writes the SSR Function that serves dynamic routes.

[build]
  command = "bun run build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "20"
